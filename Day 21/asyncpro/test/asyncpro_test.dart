import 'package:asyncpro/asyncpro.dart';
import 'package:test/test.dart';

void main() {
  test('Sumtest', () {
    expect(sum(10,20), 30);
    expect(sum(11,20), 31);
    expect(sum(10,2), 12);
  });
   test('ss', () {
    expect(sum(10,20), 30);
    expect(sum(11,20), 31);
    expect(sum(10,2), 12);
  });
}
